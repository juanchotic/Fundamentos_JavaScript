var miCadena = "Esta es una frase simple";
alert(miCadena.length);

var miColeccion = [10,20,"test",true,99];
alert(miColeccion.length); // 0 1 2 3 4 long = 5

// METODOS DE LAS COLECCIONES

miCadena.toUpperCase();
miColeccion.reverse(); // Dar la vuelta a la Matriz
miColeccion.sort(); // Reordena alfabeticamente los elementos
miColeccion.pop(); // Toma el ultimo valor y se lo asigna 
miColeccion.push(123); // Añade el elemento a la colección


// Manual donde se explique los elementos.